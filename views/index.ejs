<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>We're learning HTMX</title>
  <style>
    html {
      background-color: hsl(0, 4%, 9%);
      color: antiquewhite;
    }

    .reviews-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(236px, 1fr));
      gap: 1rem;
      align-items: start;
      margin-bottom: 1rem;
      max-width: 768px;
      margin: 1rem auto;
    }

    .review {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #111;
      border-radius: .25rem;
      transition: 125ms transform ease-out;


      &:hover {
        transform: scale(1.01);
      }

      .review__image-container {
        height: 10rem;
        width: full;
        background-color: #222;
        margin-bottom: 1rem;
      }

      .review__image {
        display: block;
        width: 100%;
      }

      .review__name {
        margin: 0;
        padding: 1rem 1rem 1rem 1rem;
      }

      .review__rating,
      .review__text {
        padding: 1rem 1rem;
        margin: 0;
      }
    }

    .review:hover {
      filter: brightness(110%);
    }

    .button-load-more {
      border-radius: 0.125rem;
      background-color: #111;
      border: .125rem #eee solid;
      color: #eee;
      padding: .125rem .5rem;
      margin: 0 auto;
      display: flex;
      font-size: 1.1rem;
      transition: 400ms transform;

      &:hover {
        transform: scale(1.05);
      }
    }
  </style>

  <script src="https://unpkg.com/htmx.org@1.9.5"></script>
</head>

<body>
  <h1>Is that HTMX?! Have I done it?</h1>
  <script>
    console.log("👀")
  </script>

  <div class="reviews-grid">
    <% reviews.forEach(review=> { %>
      <div class="review">
        <% if (review.photoLink) { %>
          <div class="review__image-container" style="background-image: url(<%= review.photoLink %>)"></div>
          <% } %>

            <h3 class="review__name">
              <%= review.name %>
            </h3>

            <h4 class="review__rating">
              <%= review.rating %>
            </h4>

            <p class="review__text">
              <%= review.reviewText %>
            </p>
      </div>
      <% }); %>

        <div id="loadBeforeMe" style="display: none;"></div>
  </div>

  <button class='btn button-load-more' hx-get="/1" hx-target="#loadBeforeMe" hx-swap="beforebegin">
    Load next page
  </button>
</body>

</html>